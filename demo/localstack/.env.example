export AWS_ACCESS_KEY_ID=test
export AWS_SECRET_ACCESS_KEY=test
# Optional: sign up at https://app.localstack.cloud to get a free auth token.
# Adding it lets you see this session on the LocalStack dashboard.
# export LOCALSTACK_AUTH_TOKEN=your-token-here
