# No auth token needed — this demo uses LocalStack 3 (community edition).
# All services here (DynamoDB, SQS, SNS, SSM, Secrets Manager, Lambda, CloudWatch Logs)
# are available in the free community edition without registration.
