export AWS_ACCESS_KEY_ID=test
export AWS_SECRET_ACCESS_KEY=test
# Get a free auth token at https://app.localstack.cloud — required for localstack:stable.
# Lets you see this session on the LocalStack dashboard.
export LOCALSTACK_AUTH_TOKEN=your-token-here
