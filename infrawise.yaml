project: payments-service
aws:
  profile: default
  region: ap-south-1
dynamodb:
  includeTables:
    - Orders
    - Payments
    - Users
postgres:
  enabled: true
  connectionString: postgresql://localhost:5432/payments
analysis:
  sampleSize: 100
